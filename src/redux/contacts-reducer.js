let initialState = {
  email: 'harchenkodmitrii@bk.ru',
  address: 'Russia, Saint-Petersburg',
  socials: {
    vk: 'https://vk.com/harchenkodmitrii',
    gitHub: 'https://github.com/HarchenkoDmitry'
  }
};

const contactsReducer = (state = initialState, action) => {
  return state;
};

export default contactsReducer;
